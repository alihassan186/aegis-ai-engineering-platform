# v0.3 FR → code → tests

**As of:** 2026-09-02 · Steps 2.1–2.6  
**Purpose:** Map each v0.3 functional requirement to files that exist in this repo. Not a tutorial.

FR text: [functional-requirements.md](../requirements/functional-requirements.md).

| FR | What it means in this repo | Primary code paths | Primary tests |
| --- | --- | --- | --- |
| FR-080 | In-process catalog of five services (`user`, `order`, `payment`, `inventory`, `notification`). Status is data (`healthy` / `degraded` / `down`). Simulator process is separate from AEGIS (`GET /health`, `GET /services`). | `apps/simulator/main.py` · `apps/simulator/config.py` · `apps/simulator/services/catalog.py` · `apps/simulator/services/runtime.py` | `tests/unit/simulator/test_health.py` · `tests/unit/simulator/test_service_catalog.py` · `tests/unit/simulator/test_config.py` |
| FR-081 | Each service can emit a structured log, metric sample, and trace span (plus optional deployment event). Sink is an in-memory buffer (`POST /signals/tick`, `GET /signals`). | `apps/simulator/signals/models.py` · `apps/simulator/signals/emitter.py` · `apps/simulator/main.py` (`/signals*`) | `tests/unit/simulator/test_signals.py` |
| FR-082 | One named scenario at a time, activated from HTTP or `SIMULATOR_SCENARIO`. Bias is config, not real faults. `GET/POST/DELETE /scenarios`. | `apps/simulator/scenarios/engine.py` · `apps/simulator/scenarios/catalog.py` · `apps/simulator/config.py` · `apps/simulator/main.py` (`/scenarios*`) | `tests/unit/simulator/test_scenarios.py` (`test_activating_a_second_scenario_replaces_the_first`, `test_deactivate_returns_all_services_to_healthy`, `test_boot_config_activates_one_scenario`, `test_get_scenarios_lists_ids_and_active_flag`) |
| FR-083 | Six named ids: `db_exhaustion`, `memory_leak`, `latency_spike`, `bad_deployment`, `queue_backlog`, `dependency_failure`. | `apps/simulator/scenarios/catalog.py` · `apps/simulator/scenarios/engine.py` | `tests/unit/simulator/test_scenarios.py` (`test_all_fr083_ids_exist_in_the_catalog` and one test per named scenario) |
| FR-084 | Simulator posts a signed incident signal to AEGIS when a scenario is active (`POST /emit`). Client is stdlib HTTP + HMAC; it does not import `aegis.api`. | `apps/simulator/aegis_client.py` · `apps/simulator/main.py` (`POST /emit`) · `apps/simulator/config.py` | `tests/unit/simulator/test_aegis_client.py` |
| FR-113 | AEGIS accepts `POST /api/v1/webhooks/incidents`. Authenticity is HMAC-SHA256 (`X-Aegis-Signature`), not JWT. Use case `IngestIncidentSignal`. | `src/aegis/api/webhooks/router.py` · `src/aegis/api/webhooks/signature.py` · `src/aegis/api/webhooks/schemas.py` · `src/aegis/application/incidents/ingest_signal.py` | `tests/integration/api/test_webhook_ingest.py` · `tests/unit/api/test_webhook_auth.py` · `tests/contract/test_openapi_incidents.py` |
| FR-007 | Same `affected_service` + scenario + UTC calendar hour collapse into one **open** incident. AEGIS computes `v1\|{service}\|{scenario}\|{hour}`. Duplicate ingest: HTTP 200, same id. Closed rows do not match. Manual create stays `fingerprint=NULL`. | `src/aegis/domain/incidents/fingerprint.py` · `src/aegis/application/incidents/ingest_signal.py` · `src/aegis/domain/incidents/entity.py` (`note_duplicate_signal`) · `src/aegis/infrastructure/repositories/incident_repository.py` (`get_open_by_fingerprint`) · `alembic/versions/a3f91c2d4e80_incident_fingerprint.py` | `tests/unit/domain/incidents/test_fingerprint.py` · `tests/unit/application/incidents/test_ingest_signal.py` (`test_second_ingest_returns_same_id`) · `tests/integration/api/test_webhook_ingest.py` (`test_duplicate_webhooks_return_the_same_open_incident`) · `tests/integration/repositories/test_incident_repository.py` (`test_get_open_by_fingerprint_ignores_closed_rows`) |

## Out of v0.3

- No RAG / OpenSearch / embeddings
- No agents / LangGraph / Bedrock investigation
- No EventBridge / SQS / `incident.opened.v1`
- No related-incident graph (FR-008)
- No webhook IP allowlisting (THR-002 remainder)
